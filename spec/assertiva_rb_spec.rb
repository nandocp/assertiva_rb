# frozen_string_literal: true

RSpec.describe AssertivaRb do
  it "has a version number" do
    expect(AssertivaRb::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
